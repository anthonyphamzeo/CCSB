# Architecture

Maintain current context, container, component, sequence, solution-layering, deployment, and environment-topology views here. Diagrams should identify trust boundaries, authoritative data, synchronous limits, asynchronous hand-offs, failure paths, and operational ownership.

Use `decisions/ADR-000-template.md` for durable architectural choices.

