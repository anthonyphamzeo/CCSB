# Release Task Lists

Create one directory per release, for example `release-01-mvp/` or `v1.0.0/`. Each release record must state scope, ordered work items, entry/exit criteria, dependencies, evidence links, deployment decision, and rollback readiness.

