# Tools

- `validation/`: schema, contract, dependency, solution, and release validators.
- `packaging/`: deterministic solution and artefact packaging helpers.
- `provisioning/`: controlled development/test provisioning utilities.
- `developer/`: local setup and quality-of-life commands.

Tools must support non-interactive CI execution, clear exit codes, `--help`, and secret-safe logging.

