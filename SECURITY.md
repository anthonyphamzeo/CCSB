# Security

Do not report vulnerabilities in public issues. Contact the repository owner through an approved private channel.

Never commit tenant credentials, client secrets, certificates, connection strings, access tokens, production data, or sensitive telemetry. Use managed identity where available, approved secret storage otherwise, and least-privilege Dataverse/Azure roles.

Security-sensitive changes require threat assessment, authorization tests, audit/telemetry review, and explicit deployment and rollback notes.

