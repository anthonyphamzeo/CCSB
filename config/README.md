# Configuration

Per-environment folders contain templates and documented differences only. Values such as URLs may be supplied during deployment; secrets belong in an approved vault and are referenced through managed identity or secure pipeline variables.

Commit `.example` templates, not live credentials or tenant exports.

