# CCSB.Plugins.Tests

Unit tests for plug-in adapters and context guards.

Use Dataverse test doubles only for execution-context behavior. Business rules should be tested in `CCSB.Application.Tests`.
